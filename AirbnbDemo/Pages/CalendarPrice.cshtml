@page
@model CBTDWeb.Pages.CalendarPrice

@{
}

<form method="Post" asp-page="Index">
    <input type="hidden" asp-for="objPrice.PropertyId"/>
    <div class="row mb-4">
        <div class="col-md-4">
            <div class="form-group">
                <label asp-for="objPrice.StartDate">Start Date</label>
                <input asp-for="objPrice.StartDate" type="date" id="startDate" name="startDate" class="form-control" value="@Request.Query["startDate"]"/>
            </div>
        </div>
        <div class="col-md-4">
            <div class="form-group">
                <label asp-for="objPrice.EndDate">End Date</label>
                <input asp-for="objPrice.EndDate" type="date" id="endDate" name="endDate" class="form-control" value="@Request.Query["endDate"]"/>
            </div>
        </div>
        <div class="col-md-4">
            <div class="form-group">
                <label for="amount">Enter amount:</label>
                <input
                    type="number"
                    id="amount"
                    name="amount"
                    step="0.01"
                    min="0"
                    placeholder="0.00"
                    aria-label="Amount in dollars">
            </div>
        </div>
        <div class="col-md-4 d-flex align-items-end">
            <button type="submit" class="btn btn-primary">Submit</button>
        </div>
    </div>
</form>
